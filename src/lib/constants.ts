/**
 * Phase 6 — Placeholder editor identity until Firebase Auth
 *
 * Written into lastEditedBy on every create/update in patients.ts.
 * Replace with request.auth.uid (or email) when login is added.
 */
export const PLACEHOLDER_EDITOR_ID = "dev-dashboard@finni.local";
