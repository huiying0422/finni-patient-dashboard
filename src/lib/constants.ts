/**
 * Placeholder editor identity until Firebase Auth is wired up.
 * Replace with request.auth.uid (or a mapped provider profile id) in production.
 */
export const PLACEHOLDER_EDITOR_ID = "dev-dashboard@finni.local";
